import React,{useMemo} from 'react';
export default function TopographicField({intensity=1}){const paths=useMemo(()=>Array.from({length:18},(_,i)=>{const y=80+i*42;let d=`M -80 ${y}`;for(let x=-80;x<=1580;x+=80){const w=Math.sin(x*.012+i*.63)*(18+i*1.25)+Math.cos(x*.0045-i)*12;d+=` L ${x} ${y+w}`}return d}),[]);return <svg className="topo" viewBox="0 0 1440 900" preserveAspectRatio="none" aria-hidden="true"><defs><linearGradient id="lineFade" x1="0" x2="1"><stop offset="0%" stopColor="rgba(255,255,255,0)"/><stop offset="50%" stopColor={`rgba(255,255,255,${.14*intensity})`}/><stop offset="100%" stopColor="rgba(255,255,255,0)"/></linearGradient></defs>{paths.map((d,i)=><path key={i} d={d} fill="none" stroke="url(#lineFade)" strokeWidth="1"/>)}</svg>}
